[package]
name = "redamon"
version = "0.1.0"
edition = "2021"
description = "RedAmon — Continuous automated security red teaming for Digiquarium"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = "0.4"
